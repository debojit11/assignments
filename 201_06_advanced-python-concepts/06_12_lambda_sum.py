# Write a lambda expression that takes in three numbers
# and returns the sum of the numbers.

# Lambda expression to sum three numbers
sum_three_numbers = lambda x, y, z: x + y + z

# Example usage
result = sum_three_numbers(5, 10, 15)
print(result)
